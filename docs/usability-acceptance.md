# 普通用户视角验收（2026-09-11）

## 方法与结论

本轮从 Finder 桌面入口启动，在现有 Chrome 中逐步点击、输入、切换页面并阅读实际结果。没有注入 `window.state`、伪造接口返回或直接调用生成 API 代替 UI。AI 流程使用内置虚构示例和实际配置的 Grok；真实聊天没有发给云端。

**结论：基础资料工作流可操作；画像内容质量未达成产品目标，不能以技术测试通过替代产品验收。**

## 分项记录

| 项目 | 本轮结果 |
|---|---|
| 桌面入口 → 正式档案 → 搜索并选择好友 | 实际 UI 通过 |
| 真实好友发送预览 → 暂不发送 | 实际 UI 通过；未批准发送真实数据 |
| 正式收藏会话选择 → 整理 → 本地导出 | 实际 UI 通过；导出文件另做完整性检查 |
| 内置示例 → 身份确认 → 我的画像/好友画像 → 实际 Grok 返回 | 实际 UI 通过，无模拟模型 |
| 查看依据、重开历史报告、导出报告 | 实际 UI 通过 |
| 示例条目 → 写笔记 → 已读 → 离开再打开 | 实际 UI 通过 |
| 生成中切页返回 | 曾失败；修复后在真实 UI 中看到自动恢复进度且生成按钮禁用 |
| 从历史查看进度 → 完成报告 | 曾要求刷新；修复后实际 UI 就地显示报告 |
| 返回原聊天的昵称与时间信息 | 曾丢失；修复后实际 UI 显示正确昵称和时间 |
| 打开 Finder 内的导出文件、打开离线 HTML | **未完成 UI 验收**：系统窗口工具无法取得 Finder/Chrome 原生窗口，浏览器策略禁止本地 file URL；没有绕过 |
| 有价值的画像归纳 | **未通过**：实际结果目前主要是原话分类，不是完整、有上下文的画像 |

## 本轮修复

- 页面重新进入时，按画像类型及准确范围查找正在运行的任务，恢复进度，不重新发起模型请求。
- 停止为已经移除的视图继续轮询；生成中禁用引擎变更。
- 历史任务完成后直接加载报告，状态标题也随任务状态同步。
- 引用跳转使用真实会话元数据，避免昵称退化为内部 ID、时间为空。
- 已读条目重新打开后，已读按钮仍为禁用态。

## 尚需完成的产品工作

1. 将可追溯原话与待核对的 AI 归纳分层，提供有范围、上下文和局限说明的摘要，而不是把一句原话重新贴在画像标题下。
2. 改进资料不足时的反馈，避免把零散寒暄包装成有用画像；分类本身也应标清需要核对。
3. 提供适合体验产品的中文示例和完整示例媒体。当前内置测试样本很小，且有故意缺失的附件，不能承担完整产品展示。
4. 在可操作的系统窗口环境中补齐“从结果按钮打开目录，再打开离线网页”的实际 UI 验收。

这些项目仍为待办，没有把未来能力写成已经完成。
